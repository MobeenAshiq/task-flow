FROM node:20-alpine

# Security: Create non-root user
RUN addgroup -S sandboxgroup && adduser -S sandboxuser -G sandboxgroup

# Set sandbox directory
WORKDIR /sandbox

# Install basic runtimes
RUN apk add --no-cache python3 g++ make

USER sandboxuser

CMD ["node", "-e", "console.log('Sandbox ready')"]
